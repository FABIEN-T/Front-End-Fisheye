async function sorting(media) {
    // const { title, likes, date } = dataMedia; 
    
    // const monObjet = {media};
    // const monTableau = Object.keys(monObjet).map(function(cle) {
    //     return [Number(cle), monObjet[cle]];
    // });
    // console.log("tab", monTableau);

    const tab = Array.of(media);
    console.log("tab", tab);
}